namespace APP.Features.Placeholders
{
    // 预留页暂时只吃标题参数，逻辑还没长出来，所以这里先保持空壳。
    // Placeholder screens only need a title for now, so the view model intentionally stays empty until real behavior arrives.
    public class PlaceholderViewModel
    {
    }
}
